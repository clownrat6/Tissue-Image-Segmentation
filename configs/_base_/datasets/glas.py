# dataset settings
dataset_type = 'GlasDataset'
data_root = 'data/glas'
process_cfg = dict(
    if_resize=True,
    if_flip=True,
    if_jitter=True,
    if_elastic=True,
    if_blur=True,
    if_crop=True,
    if_pad=True,
    if_norm=False,
    min_size=768,
    max_size=2048,
    resize_mode='fix',
    edge_id=2,
)
data = dict(
    samples_per_gpu=4,
    workers_per_gpu=4,
    train=dict(
        type=dataset_type,
        data_root=data_root,
        img_dir='train/c0',
        ann_dir='train/c0',
        split='train_c0.txt',
        process_cfg=process_cfg),
    val=dict(
        type=dataset_type,
        data_root=data_root,
        img_dir='testA/c0',
        ann_dir='testA/c0',
        split='testA_c0.txt',
        process_cfg=process_cfg),
    test=dict(
        type=dataset_type,
        data_root=data_root,
        img_dir='testA/c0',
        ann_dir='testA/c0',
        split='testA_c0.txt',
        process_cfg=process_cfg),
)
